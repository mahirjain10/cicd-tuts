name: CI Checkpoint1

on:
  push: 
    branches: [main]
  pull: 
    branches: [main]
jobs :
  run-checkpoint :
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - run: echo "$GITHUB_REF"
      - run: echo "checkpoint 1 done"
